export enum UserEnum {
  DEFAULT_USER = 'USER',
  ADMIN_USER = 'ADMIN',
  OPERATIONAL_USER = 'OPERATIONAL',
}

export enum Status {
  CONFIRMED = 'confirmed',
  PENDING = 'pending',
  CANCELLED = 'cancelled',
}
